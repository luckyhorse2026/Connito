# -----------------------------
# PyTorch (install CUDA wheel from pytorch.org first)
# -----------------------------
torch==2.10.0

# -----------------------------
# Core & Utilities
# -----------------------------
pydantic-settings==2.2.1
structlog==24.1.0
prometheus-client==0.20.0
psutil==6.1.1
PyYAML==6.0.1
# btcli 9.23 requires typer~=0.26 (fresh-resolve conflict; bumped with the bittensor 10.5 move)
typer==0.26.0
python-dotenv==1.1.0
cryptography==46.0.5
varint==1.0.2
multihash==0.1.1

# -----------------------------
# ML stack
# -----------------------------
transformers>=4.45,<5
datasets==3.0.0
huggingface_hub>=0.24,<1
tokenizers>=0.20,<0.22
safetensors==0.4.5
torchdata==0.11.0
scipy==1.15.3
prefetch_generator==1.0.3
unsloth @ git+https://github.com/unslothai/unsloth.git; platform_system == "Linux"

# -----------------------------
# API / server (validator)
# -----------------------------
fastapi==0.110.1
uvicorn[standard]==0.30.0
# bittensor 10.5 requires requests>=2.33 (fresh-resolve conflict caught by CI 2026-07-21)
requests==2.33.0
aiofiles==25.1.0
python-multipart==0.0.22

# -----------------------------
# Distributed training
# -----------------------------
git+https://github.com/learning-at-home/hivemind.git@4bd43b77
py-cid==0.5.0

# -----------------------------
# Storage / filesystem
# -----------------------------
fsspec==2024.6.1

# -----------------------------
# Blockchain
# -----------------------------
# 10.1 predates the chain's multi-mechanism runtime migration: its storage-key
# encoding sends netuid as a bare int where the runtime now expects the NetUid
# newtype, so metagraph()/btcli-overview fail against live finney+archive.
bittensor==10.5.0
bittensor-cli==9.23.2
# Both pins above require the cyscale-based async-substrate-interface 2.x,
# which fails at import if py-scale-codec is also installed — do not
# reintroduce a scalecodec pin alongside these.
async-substrate-interface==2.2.1

# -----------------------------
# Observability
# -----------------------------
wandb==0.25.1
numpy==2.0.2
pandas==2.2.3

# ------------------------------
# Dev & Testing
# ------------------------------
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
hypothesis==6.112.0

ruff==0.6.3
black==24.8.0
mypy==1.11.1
types-PyYAML==6.0.12.20240808

# Grafana dashboard generation (run `generate-dashboard` to emit JSON).
grafanalib==0.7.1